<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container_drink_item"
    android:layout_width="match_parent"
    android:layout_height="150dp"
    android:layout_margin="4dp"
    android:padding="10dp"
    tools:background="@drawable/selector_soju_bg">

    <ImageView
        android:id="@+id/iv_drink_item"
        android:layout_width="96dp"
        android:layout_height="96dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/tv_drink_item"
        tools:ignore="ContentDescription"
        tools:src="@drawable/selector_soju_img" />

    <TextView
        android:id="@+id/tv_drink_item"
        style="@style/Title4"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@drawable/selector_soju_btn"
        android:gravity="center"
        android:paddingVertical="10dp"
        android:textColor="@drawable/selector_drink_text_color"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:text="소주" />

</androidx.constraintlayout.widget.ConstraintLayout>
